
const mainEndpoint = "http://localhost:8080/api/"

export const songsEndpoint = `${mainEndpoint}songs`;

export const usersEndpoint = `${mainEndpoint}users`;

export const playlistsEndpoint = `${mainEndpoint}playlists`;

